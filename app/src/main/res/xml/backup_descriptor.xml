<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- TODO Remove the following "exclude" elements to make them a part of the auto backup -->
    <!-- Exclude the shared preferences file that contains the GCM registrationId -->
    <exclude domain="sharedpref" path="string.xml"/>
    <exclude domain="sharedpref" path="customtheme.xml"/>
    <exclude domain="sharedpref" path="boolean.xml"/>
    <exclude domain="sharedpref" path="agreements.xml"/>
    <exclude domain="sharedpref" path="setting.xml"/>
</full-backup-content>
